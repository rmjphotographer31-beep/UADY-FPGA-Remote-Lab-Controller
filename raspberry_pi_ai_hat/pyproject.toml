[project]
name = "uady-fpga-pi"
version = "5.4.0"
description = "Raspberry Pi-side manager for the UADY FPGA Remote Lab Controller v5.4"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]

[tool.ruff]
line-length = 120
